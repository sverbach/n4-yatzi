{
  "name": "yatzy-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Minimal Bun static server for the Yatzy client build",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts",
    "serve": "cd ../client && npm run build && cd ../server && bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
